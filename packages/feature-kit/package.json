{
  "name": "feature-kit",
  "version": "1.0.0",
  "description": "LifeOS feature backlog — data, UI, and CLI wrapper around featmap",
  "private": true,
  "main": "src/loader.ts",
  "scripts": {
    "list": "ts-node src/cli.ts list",
    "regen": "ts-node src/cli.ts regen",
    "migrate": "ts-node scripts/migrate-from-markdown.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "peerDependencies": {
    "react": ">=17.0.0",
    "react-native": ">=0.70.0"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true },
    "react-native": { "optional": true }
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0"
  }
}
